cmake_minimum_required(VERSION 3.25)
project(OrderbookSim)

# Add pybind11 subdirectory
add_subdirectory(pybind11)

# Create the Python extension module
pybind11_add_module(orderbook orderbook_pybind.cpp)
